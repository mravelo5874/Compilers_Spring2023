// expected result: 1

int try_AND(int a, int b)
{
    int res = (-1);
    res = (a & b);
    return res;
}

int try_OR(int c, int d)
{
    int res = (-1);
    res = (c | d);
    return res;
}

int main()
{
    int and = try_AND(2, 4);
    int or = try_OR(1, 3);
    return try_OR(and, or);
}